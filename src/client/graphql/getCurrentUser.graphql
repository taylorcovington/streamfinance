query GetCurrentUser {
  currentUser {
    id
    name
    email
    role
    accountStatuses {
      id
      onboarding
      income
      necessities
      savings
      investments
    }
    projects {
      id
      name
      slug
      paidPlan
    }
  }
}
