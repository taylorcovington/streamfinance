mutation UpdateUser($name: String, $email: String!, $userId: String!) {
  updateUser(name: $name, email: $email, userId: $userId) {
    id
    name
    email
    projects {
      id
      name
      slug
      paidPlan
    }
  }
}
